---
type: meta
status: active
date: 2026-07-21
description: "Index for durable realtime-video research notes, including the accepted coherent-displacement scorer and deterministic experiment-batch comparison layer."
---

# Research — Index

Durable source comparisons and findings that outlive one experiment:

- [[Unsloth-Puzzles-Systems-Study]] — the five-task Unsloth hiring challenge as
  an ML-systems curriculum; why its environment has decayed, why Task E's
  chunked autograd remains the most valuable idea, and which benchmark/compiler
  lessons transfer to realtime-video.
- [[Pegasus-1.5-Video-Judge-Calibration]] — official Pegasus 1.5 structured
  video-analysis contract, strict local evidence boundary, and the transport-GO
  / calibration-NO-GO result from three bounded fox canaries.
- [[Gemini-3.1-Video-Judge-Calibration]] — explicit-16-fps Gemini 3.1 Pro
  transport, exact-byte two-pass repeatability, the apples-to-apples Pegasus
  disagreement, and the human-anchored qualification thresholds.
- [[Pinned-CF1-CUDA-Bootstrap]] — the byte-complete minimal CF++1,
  rolling-TAEHV, and Wan-text stack; its fail-closed asset gate, direct-loader
  bootstrap, frozen H100 identity, and completed real-CUDA smoke/worker proof.
- [[CF1-H100-Runtime-Preflight]] — the frozen schema-v2 H100 runtime/evidence
  lock, exact tokenizer sentinel, OCI/Python/package/native identity,
  cgroup/HBM admission, actual FA2/Torch provenance, and successful unchanged
  admission on the dedicated worker.
- [[CF1-Rolling-TAEHV-Session]] — the fail-closed rolling decoder contract:
  full runtime/guard identity, explicit CUDA stream and event ownership,
  corrected three-latent trims, synchronized D2H, immutable raster payloads,
  and the completed 21-chunk/81-PNG H100 and persistent-worker proofs.
- [[CF1-Latent-Pull-and-Smoke]] — the exact 45-forward per-block generator,
  frozen H100 admission, completed one-block and full 21-block/81-PNG serial
  proofs, the 1,649-file bytecode cleanup/replay, and honest non-benchmark
  timing/poison semantics.
- [[CF1-Development-Video-Artifact]] — exact 81-PNG-to-H.264 assembly with
  double encoding and full decode, plus the crash-durable at-most-once Gemini
  and TwelveLabs development-understanding result for the first real smoke;
  provider disagreement remains development-only and non-gating.
- [[CF1-Persistent-CUDA-Worker]] — the separate warm-only real-worker scaffold:
  verified pre-HELLO boot, one latent pull/decode per credit, clean-completion
  reuse, provenance-bound launch, accepted same-worker two-seed execution, and
  forced-death poison/reap plus the accepted real-browser presentation path.
- [[CF1-Prompt-Conditioned-Motion]] — exact same-worker evidence that terse
  bouncing-ball failure is prompt-conditioned rather than frozen delivery, and
  the disclosed temporal compiler's live 106-word, three-bounce CF++1 H100
  product release at 81 frames / 21 chunks / 21 ACKs.
- [[Browser-Streaming-Transport]] — hardened same-origin binary transport,
  post-paint ACK semantics, bounded replay, and the accepted single-field
  prompt UI: exact current-text generation plus optional Gemini 3.1 Flash-Lite
  enhancement, with live 81 painted frames / 21 chunks / 21 ACKs and replay
  left visible at loopback.
- [[Coherent-Displacement-Metric]] — local CoTracker3/DINOv2 measurement of
  primary-object travel with two-space camera compensation, adjacent-frame
  anti-flicker guards, red-first synthetic gates, and an honest ten-independent-
  content P0 calibration.
- [[Displacement-Batch-Comparison]] — deterministic condition-by-prompt batch
  matrices around the unchanged displacement scorer, with complete per-clip
  JSON, explicit holes, pairwise B-minus-A verdicts, and exact P0 reproduction.

The initial measured H100 record remains in `[[State]]`,
[[ADR-001-quality-qualified-headline]], and
[[session-1-h100-gate-and-second-brain]].
